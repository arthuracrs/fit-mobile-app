import { SignInAsStudent, SignInAsTrainer, SignUpAsStudent, SignUpAsTrainer } from "./localAuth/localAuth"

export const auth = {
    SignInAsStudent,
    SignInAsTrainer,
    SignUpAsStudent,
    SignUpAsTrainer
}